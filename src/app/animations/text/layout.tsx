'use client'

import AnimationsSidebar from '@/components/animations-sidebar'

export default function TextAnimationsLayout({ children }: { children: React.ReactNode }) {
  return <AnimationsSidebar>{children}</AnimationsSidebar>
}
